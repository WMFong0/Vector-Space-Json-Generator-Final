
# Backend — Unified CTAs (no black) + Black JSON Block

- All four CTAs on the Results page now use a light style (no black).
- Generated JSON remains on a black background for visibility.

Run
```bash
pip install -r requirements.txt
python app.py
# open http://localhost:5000
```
